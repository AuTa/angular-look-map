import { PageMarkerDirective } from './page-marker.directive';

describe('PageMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new PageMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
