import { AutoGrowTextAreaDirective } from './auto-grow-text-area.directive';

describe('AutoGrowTextAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoGrowTextAreaDirective();
    expect(directive).toBeTruthy();
  });
});
