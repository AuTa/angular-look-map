import { LatLngMarkerDirective } from './lat-lng-marker.directive';

describe('LatLngMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new LatLngMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
